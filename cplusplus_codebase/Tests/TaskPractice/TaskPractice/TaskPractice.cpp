
/*	Experimenting with different task setups
	Start date	08 Oct 2015
	Author		Oliver Stanley
	Group		Moritz lab, UW Dept. Rehab Medicine	*/

#include <TaskPractice.h>

//max distance so far = 0;

int main(int argc, char *argv[])
{
	//get distance
	//is distance max OR is distance at a new ring/sphere? (mitigate noise)
	//decay of distance to reward? (might jsut encourage laziness)
	//how to incorporate directionality?
	//     shells of constant area?
	//     shells of constant degrees? (gives set number of targets)
	//apply force? if so w/ damping

	printf("Hello world");
	_getch();
}
